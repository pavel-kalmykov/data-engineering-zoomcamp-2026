[project]
name = "zoomcamp-batch"
version = "0.1.0"
description = "Module 6 - Batch Processing with Spark"
requires-python = ">=3.10"
dependencies = [
    "pyspark>=4.1.0",
    "jupyter>=1.1.0",
    "jupyter-ruff>=0.3.1",
]
